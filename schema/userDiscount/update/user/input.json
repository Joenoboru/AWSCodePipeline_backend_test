{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "description": "PUT /userDiscount/:id UserDiscount.update input data",
  "properties": {
    "use": {
      "type": "boolean",
      "description": "使用折扣",
      "enum": [true]
    }
  }
}